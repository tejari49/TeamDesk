name: Build APK (AGP 9.0.1 - Stable)

on:
  push:
    branches: ["main", "master"]
  pull_request:
  workflow_dispatch:

jobs:
  build-apk:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: "temurin"
          java-version: "17"

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: Install Android Packages
        run: |
          yes | sdkmanager --licenses
          sdkmanager "platforms;android-36" "build-tools;36.0.0" "platform-tools"

      - name: Extract PlanWise.zip
        run: |
          if [ ! -f "PlanWise.zip" ]; then
            echo "❌ ERROR: PlanWise.zip not found!"
            exit 1
          fi
          unzip -o -q PlanWise.zip
          if [ ! -d "PlanWise" ]; then
            echo "❌ ERROR: PlanWise directory not created!"
            exit 1
          fi
          echo "✅ PlanWise extracted successfully"

      - name: Make Gradle Wrapper Executable
        run: |
          chmod +x ./PlanWise/gradlew
          echo "✅ Gradle wrapper made executable"

      - name: Build Debug APK
        run: |
          cd PlanWise
          ./gradlew clean assembleDebug

      - name: Build Release APK
        run: |
          cd PlanWise
          ./gradlew assembleRelease

      - name: Upload Debug APK
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: planwise-debug
          path: PlanWise/app/build/outputs/apk/debug/*.apk

      - name: Upload Release APK
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: planwise-release
          path: PlanWise/app/build/outputs/apk/release/*.apk
          
