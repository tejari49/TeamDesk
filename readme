# im lokalen Repo
git rm -r --cached .
rm -rf * .[^.]* 2>/dev/null || true

# minimale Datei, damit Repo nicht "leer" ist (optional)
echo "# empty" > README.md

git add -A
git commit -m "wipe repo contents"
git push origin HEAD
